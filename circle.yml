## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  # Version of ruby to use
  ruby:
    version:
      1.8.7-p358-falcon-perf
  node:
    version:
      12.0

## Customize dependencies
dependencies:
  pre:
    - npm install -g gulp cordova ionic # install from a different package manager
