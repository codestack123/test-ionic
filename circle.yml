## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  node:
    version: 0.11.8

## Customize dependencies
dependencies:
  pre:
    - npm install gulp cordova ionic # install from a different package manager
